# program accept 4 digit comma separated binary numbers and check whether divisible by 5

def conversion(n):
    return int(n,2)
n = input("enter binary number ")
n=n.replace(",","")
n = conversion(n)
if(n%5 is 0):
    print("YES,divisible by 5")
else :
    print("NO,not divisible by 5")